# initial setups
```
1) install node js (if not installed).
2) update your email credentials (this app is for gmail only). 
3) add a to, from, text, subject in app.js file.
4) run 'npm install'.
5) run 'npm start'.
6) now go to localhost:3000 or 127.0.0.1:3000
```